# Ambient audio placeholder

Drop a looping MP3 here as `ambient.mp3`.

The theme references `assets/dist/audio/ambient.mp3` after `npm run prod`.
If the file is missing, `app.js` falls back to a Web Audio ambient loop on first interaction.

Recommended: short seamless loop, ~30–60s, normalized loud.
